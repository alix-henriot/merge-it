# Merge It

### TO DO LIST

- Change TicketCard background color when it is the active ticket in view ✅
- Improve mergeTickets() response and toast response handler ✅
- Update UI optimistically on Merge ✅
- Add loading state TicketMergeButton() ✅
- Show TicketButtonGroup only when ticket is mergeable (not closed and not active ticket) ✅
- Add Skeleton ✅
- Hover card with ticket comments ✅
- When active ticket is closed, do not render TicketMergeButton for all tickets. (conditions for merging: active ticket is not closed and target source is not closed either). ✅
- Add Zendesk OAuth ✅
- Handle dynamic Zendesk subdomain (currently hardcoded) ✅
- Add mergeTickets Error message granularity ✅
- Messaging for auth status ✅
- Refactor ZAFClient derivated Hooks into one useZendesk() 🕦
- Add confirmation drawer on Merge ✅
- Add subdomain to session ✅
- Improve plain html comment body ✅
- Use moment for dates
- Improve authenticated API routes
- Post messages to all iframes to update tickets array ✅
- Add Tooltip for Copy button ✅
- Add tag option on merge ?
- Add satisfaction ✅
- Write tests 🕦